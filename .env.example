# Postgres (Neon / Vercel Postgres / Railway)
DATABASE_URL=postgres://user:pass@host/db?sslmode=require

# Mật khẩu trang /login. Bỏ trống = tắt auth (chỉ nên dùng khi dev local).
ADMIN_PASSWORD=

# Cho phép endpoint LLM dùng http:// (Ollama local). Mặc định chỉ https.
ALLOW_HTTP_ENDPOINT=0
